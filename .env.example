# PostgreSQL bağlantı bilgileri
DB_USER=postgres
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432
DB_NAME=manifetch

# AES-256 şifreleme anahtarı (32 byte hex)
# Üretmek için: python -c "import secrets; print(secrets.token_hex(32))"
MANIFETCH_SECRET_KEY=